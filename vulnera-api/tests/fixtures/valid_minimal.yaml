openapi: 3.0.0
info:
  title: Simple API
  version: 1.0.0
  description: Minimal valid OpenAPI specification
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
