#!/usr/bin/env bash
set -euo pipefail

echo "pre-commit: if you used LLM-based tooling, disclose it in the pull request description."
cargo fmt --all --check
cargo check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --lib --tests
