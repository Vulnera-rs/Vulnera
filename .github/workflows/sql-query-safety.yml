name: SQL Query Safety

on:
    pull_request:
        branches: [main]
    push:
        branches: [main]

jobs:
    sql-query-safety:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: SQLx query safety audit
              run: bash .github/scripts/sql_query_safety_audit.sh
