# Cargo configuration for test execution

[build]
# Use all available cores for building (0 means use all cores)

[test]
# Test configuration

[target.'cfg(test)']
# Test-specific target configuration
